# ****************************************************************************
# * *
# * City Council Meeting Analyzer                                            *
# * Version: 0.2.034                                                         *
# * Component: Git Exclusion Rules                                           *
# * Author: joelontheroad                                                    *
# * *
# ****************************************************************************

# --- Python Environment ---
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# --- Local Configuration ---
# We keep default.yaml in the repo as a template, 
# but ignore local overrides if you create them.
configs/local.yaml

# --- Data & Storage (CRITICAL) ---
# We ignore the actual heavy content to keep the repo small.
# The program will recreate these folders as needed.
data/staging_buffer/*
data/vault/videos/*
data/vault/transcripts/*
data/vault/reports/*

# Keep the folder structure but ignore the content
!data/staging_buffer/.gitkeep
!data/vault/videos/.gitkeep
!data/vault/transcripts/.gitkeep
!data/vault/reports/.gitkeep

# --- One-time Utilities ---
# Ignoring the migration script so it doesn't clutter the main repo
migrate_legacy_files.py

# --- System Files ---
.DS_Store
Thumbs.db
*.tmp
*.log
